import React from 'react'
import "./FormButtons.css"

export const FormButtons = ({value }) => {
  return (
    <div>
        <button type='button' className='btn' >{value}</button>

    </div>
  )
}
